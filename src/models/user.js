export default class User {
    constructor(lastname, name, campus, email, password) {
      this.lastName = lastname;
      this.name = name;
      this.campus = campus;
      this.email = email;
      this.password = password;
    }

  }
