export default class User {
    constructor(productId, state, title, description, price, adress) {
        this.productId = productId;
        this.state = state;
        this.title = title;
        this.description = description;
        this.price = price;
        this.adress = adress;
    }

}